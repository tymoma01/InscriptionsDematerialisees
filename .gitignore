.DS_STORE
node_modules/
.env
.env.local
*.log
.claude/
dist/

# Contient l'ID de souscription Azure, le resource group et les noms de ressources réels — gardé
# en local uniquement (repo public), voir redeploy.sh pour l'équivalent générique/paramétré.
scripts-azure/
